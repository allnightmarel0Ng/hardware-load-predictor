# Copy to .env and fill in your values.
# These are the only secrets needed — everything else is in config.yaml.

DATABASE_URL=postgresql://predictor:predictor@db:5432/predictor
METRICS_SOURCE_URL=http://<prometheus-host>:9090
